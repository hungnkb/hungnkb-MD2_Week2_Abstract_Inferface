export class Circle {
    radius: number;
    constructor(radius: number) {
        this.radius = radius;
    }

    getRadius() {
        return this.radius;
    }
    setRadius(radius: number) {
        this.radius = radius;
    }
}